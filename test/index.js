'use strict';
var assert = require('assert');
var rzdokuMigrScript = require('../lib');

describe('rzdoku-migr-script file-reader', function() {
  it('should read file out of configured directory', function() {
    assert(false,
      'we expected this package author to add actual unit tests.');
  });
});
